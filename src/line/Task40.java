package line;

public class Task40 {
    public static void main(String[] args){
        int x = 7;
        double one = 2*Math.pow(x,2);
        double two = 3*Math.pow(x,3);
        double three = 4*Math.pow(x,2);
        double first = -one+two-three;
        double second = one+two+three;
    }
}
